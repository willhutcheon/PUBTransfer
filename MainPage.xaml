<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PUBTransfer.MainPage">
    <ScrollView>
        <VerticalStackLayout Padding="30,0" Spacing="25">
            <!-- App Image & Title -->
            <Image Source="dotnet_bot.png"
                   HeightRequest="185"
                   Aspect="AspectFit"
                   SemanticProperties.Description="dot net bot in a race car number eight" />
            <!--<Label Text="Wills .NET MAUI Bluetooth &amp; WiFi"
                   Style="{StaticResource Headline}"
                   SemanticProperties.HeadingLevel="Level1" />-->
            <!-- === Survey Card === -->
            <!--<Frame x:Name="SurveyCard"
                   CornerRadius="12"
                   Padding="15"
                   Margin="15"
                   BackgroundColor="#2D2D2D"
                   HasShadow="True">
                <VerticalStackLayout Spacing="12">
                    
            -->
            <!-- Title -->
            <!--
            <Label Text="Survey"
                           FontSize="24"
                           TextColor="#f5f5f5"
                           FontFamily="sans-serif-medium"
                           Margin="5,0"/>
            -->
            <!-- Divider Line -->
            <!--
            <BoxView HeightRequest="2"
                             BackgroundColor="#f5f5f5"/>
            -->
            <!-- Survey Status -->
            <!--
            <Label x:Name="lblSurvey"
                        FontSize="18"
                        TextColor="#f5f5f5"
                        FontFamily="sans-serif-medium"
                        HorizontalOptions="Center"
                        Margin="0,12"/>
            -->
            <!-- Button -->
            <!--
            <Button x:Name="btnSurvey"
                            Text="View Survey"
                            BackgroundColor="#3F51B5"
                            CornerRadius="8"
                            HorizontalOptions="Center"
                            Clicked="BtnSurvey_Clicked"/>
            
                </VerticalStackLayout>
            </Frame>-->
            <!-- DEV/QA/PROD switches -->
            <VerticalStackLayout Spacing="10" HorizontalOptions="Center">
                <!--<HorizontalStackLayout Spacing="10" VerticalOptions="Center">
                    <Switch x:Name="sliderDEV" IsToggled="True"/>
                    <Label Text="Development" VerticalOptions="Center"/>
                </HorizontalStackLayout>
                <HorizontalStackLayout Spacing="10" VerticalOptions="Center">
                    <Switch x:Name="sliderQA" IsToggled="False"/>
                    <Label Text="QA" VerticalOptions="Center"/>
                </HorizontalStackLayout>
                <HorizontalStackLayout Spacing="10" VerticalOptions="Center">
                    <Switch x:Name="sliderPROD" IsToggled="False"/>
                    <Label Text="Production" VerticalOptions="Center"/>
                </HorizontalStackLayout>-->


                <!--radio buttons, use-->
                <!--<RadioButton Content="Development"
                    GroupName="EnvironmentGroup"
                    IsChecked="True"
                    Value="DEV"
                    CheckedChanged="OnEnvironmentChanged" />
                <RadioButton Content="QA"
                    GroupName="EnvironmentGroup"
                    Value="QA"
                    CheckedChanged="OnEnvironmentChanged" />
                <RadioButton Content="Production"
                    GroupName="EnvironmentGroup"
                    Value="PROD"
                    CheckedChanged="OnEnvironmentChanged" />-->
            </VerticalStackLayout>
            <!-- WebView for the survey -->
            <!--<WebView x:Name="webSurvey"
                     HeightRequest="370"
                     Margin="7"
                     HorizontalOptions="Fill"
                     VerticalOptions="Fill"/>-->
            <!--<Label x:Name="androidIdLabel" 
                TextColor="White"
                FontSize="18" />
            <Image x:Name="qrCodeImage"
                WidthRequest="300"
                HeightRequest="300" />-->
            <Button x:Name="ScanButton" 
                    Text="Scan" 
                    Clicked="OnScanClicked"/>
            <Button Text="Clear" Clicked="OnClearClicked"/>
            <!--<CollectionView x:Name="DevicesListView">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Label Text="{Binding Name}" />
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>-->



            <!--<ListView x:Name="DevicesListView"
                ItemsSource="{Binding Devices}"
                ItemTapped="OnDeviceSelected"
                HasUnevenRows="True">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Name}" Detail="{Binding Id}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>-->


            <ListView x:Name="DevicesListView"
                    ItemsSource="{Binding Devices}"
                    ItemTapped="OnDeviceSelected"
                    HasUnevenRows="True">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Padding="10">
                                <Label Text="{Binding Name}"
                                       TextColor= "#512BD4"
                                       FontSize="16" />
                                <Label Text="{Binding Id}"
                                       FontSize="12"
                                       TextColor="Gray" />
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>


            <!--<Button Text="Show Data" Clicked="OnShowDataClicked" />-->

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>